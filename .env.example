# API Keys (Required)
ANTHROPIC_API_KEY=your_anthropic_api_key_here  # Format: sk-ant-api03-...
PERPLEXITY_API_KEY=your_perplexity_api_key_here  # Format: pplx-...
OPENROUTER_API_KEY=your_openrouter_api_key_here  # Format: sk-or-...

# Model Configuration
MODEL=claude-3-7-sonnet-20250219  # Recommended models: claude-3-7-sonnet-20250219, claude-3-opus-20240229
PERPLEXITY_MODEL=sonar-pro  # Perplexity model for research-backed subtasks

# OpenRouter Configuration (Optional)
# ⚠️ WARNING: OpenRouter typically has much lower token limits than direct API access.
# The default value of 800 tokens is often insufficient for complex tasks.
# Set this to your plan's limit or keep USE_OPENROUTER=false for token-intensive operations.
OPENROUTER_MODEL=anthropic/claude-3-haiku  # OpenRouter model ID (see https://openrouter.ai/docs#models)
USE_OPENROUTER=false  # Set to true to use OpenRouter instead of direct Anthropic API
OPENROUTER_MAX_TOKENS=800  # Maximum tokens for OpenRouter responses (based on your plan limits)

# General Model Configuration
MAX_TOKENS=64000                   # Maximum tokens for model responses
TEMPERATURE=0.4                   # Temperature for model responses (0.0-1.0)

# Logging Configuration
DEBUG=false                       # Enable debug logging (true/false)
LOG_LEVEL=info                    # Log level (debug, info, warn, error)

# Task Generation Settings
DEFAULT_SUBTASKS=4                # Default number of subtasks when expanding
DEFAULT_PRIORITY=medium           # Default priority for generated tasks (high, medium, low)

# Project Metadata (Optional)
PROJECT_NAME=Your Project Name    # Override default project name in tasks.json